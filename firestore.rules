rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 책 입력 데이터: 구글 로그인한 사용자만 읽기/쓰기
    match /bookData/{docId} {
      allow read, write: if request.auth != null;
    }
  }
}
